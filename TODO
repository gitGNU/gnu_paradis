shibboleet
package sharing
tcp + upnp port forwarding
distributed hash table (¿ do we need this ?)
encryption
randomcast
makepkg
    license database

core plugins:
    uuid resolver
    uuid fetcher
    union routing
