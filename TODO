shibboleet
package sharing [PRIORITY 1]
tcp + upnp port forwarding
distributed hash table (¿ do we need this ?)
encryption
randomcast

core plugins:
    uuid resolver
    uuid fetcher
    union routing
