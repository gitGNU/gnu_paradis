shibboleet
tcp + upnp port forwarding
distributed hash table (¿ do we need this ?)
encryption
randomcast
gettext
use shared space for packages

core plugins:
    uuid resolver
    uuid fetcher
    union routing
